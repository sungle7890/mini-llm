# AI 모델 종류 정리

인공지능/머신러닝 모델을 **세 가지 축**으로 정리한 지도. 이 프로젝트에서 밑바닥부터
만든 것들(세기 모델·MLP·Transformer)을 기준점으로 삼았다.

---

## 축 1. "어떻게 배우나" (학습 패러다임)

| 종류 | 뜻 | 예 (이 프로젝트) |
|---|---|---|
| **지도학습** (Supervised) | 정답 라벨을 주고 배움 | 주식 방향예측(분류), 이미지 분류 |
| **비지도학습** (Unsupervised) | 라벨 없이 구조만 발견 | 군집화, 이상탐지 |
| **자기지도학습** (Self-supervised) | 데이터가 스스로 정답 (다음 토큰) | 언어모델(한글/bible) |
| **강화학습** (Reinforcement) | 보상으로 배움 | RLHF, 알파고, 게임 AI |

---

## 축 2. "무엇으로 만드나" (모델 구조) — 보통 "모델 종류"라고 하면 이것

### ① 고전 ML (딥러닝 이전, 지금도 강력)
- **통계/세기**: 빈도표, 나이브베이즈 — 이 프로젝트의 `01_bigram`·로또(`09/10`)
- **선형/로지스틱 회귀** — 가장 단순한 예측기
- **k-NN** (가까운 이웃 보고 판단)
- **결정트리 → 랜덤포레스트 → 그래디언트 부스팅(XGBoost·LightGBM)**
  — **표(tabular) 데이터엔 아직도 최강**. 이 프로젝트의 `17/18`이 부스팅 버전.
- **SVM** (서포트 벡터 머신)
- **k-means** (군집)

### ② 신경망 (딥러닝)
- **MLP** (다층 퍼셉트론) — 이 프로젝트의 `02_mlp`·주식 모델(`12~16`)
- **CNN** (합성곱) — 이미지에 특화
- **RNN / LSTM** — 순차 데이터(옛날 방식, 지금은 대부분 Transformer로 대체)
- **Transformer** — 이 프로젝트의 `03`, 현대 LLM(GPT·Claude)의 뼈대

### ③ 생성모델 (요즘 핵심)
- **자기회귀 (Autoregressive, GPT류)** — 텍스트를 한 토큰씩 생성. 이 프로젝트의 Transformer
- **디퓨전 (Diffusion)** — 이미지·영상 생성 (Stable Diffusion, DALL·E, Sora)
- **GAN** — 이미지 생성(옛날, 두 신경망이 경쟁)
- **VAE** — 압축·생성

---

## 축 3. "무슨 일을 하나" (과제)

분류(classification) · 회귀(regression) · 생성(generation) · 군집(clustering) ·
탐지(detection) · 분할(segmentation) · 순위/추천(ranking) · 번역(translation) …

---

## 실전 감각 — "언제 뭘 쓰나"

| 데이터/문제 | 주로 쓰는 것 |
|---|---|
| **표(엑셀형) 데이터** | 그래디언트 부스팅(XGBoost) ← 딥러닝보다 자주 이김 |
| **이미지** | CNN, (생성은) Diffusion |
| **텍스트·시퀀스** | Transformer (LLM) |
| **음성** | Transformer/CNN 계열 |
| **적은 데이터 / 빠른 baseline** | 로지스틱 회귀, 랜덤포레스트 |
| **정답 없는 탐색** | k-means, 차원축소(PCA) |

---

## 한 줄 지도

이 프로젝트에서 밑바닥부터 만든 건 **② 신경망(MLP·Transformer)**과 **① 통계(세기 모델)**,
그리고 **① 부스팅(XGBoost, `17/18`)**이다.

요즘 "AI = LLM" 이미지가 강하지만, 실제론 **표 데이터는 부스팅, 이미지는 CNN/디퓨전,
텍스트는 Transformer**처럼 문제마다 강한 모델이 다르다. "무조건 딥러닝/LLM"이 아니라
**데이터 종류에 맞는 도구**를 고르는 게 핵심.

> 그리고 이 프로젝트가 반복해서 보여준 것: **모델을 바꾸는 건 "신호가 있는데 못 뽑을 때"만
> 도움이 된다.** 신호가 없으면(복권) 어떤 모델도 소용없고, 신호가 약하면(주식) 더 강한
> 모델(XGBoost)로 바꿔도 out-of-sample은 그대로이며 오히려 과적합(착시)만 커진다.
> 관련 실험: `11_lottery_nn`(복권+신경망), `12~18`(주식 MLP·XGBoost).
