# 용어 사전 (Glossary) — 자세한 풀이 + English

이 프로젝트에서 나온 용어를 완결된 문장으로 풀어 정리했다.
형식: **한국어 (English)** — 뜻. 그리고 필요하면 *예시/왜 중요한가*.

---

## 1. 큰 그림

**언어모델 (Language Model, LM)** — "지금까지 나온 글자들을 보고, 다음에 올 글자의 확률을 매기는 함수"다. 이것 하나가 LLM의 본질이며, 나머지는 전부 이 함수를 잘 만들기 위한 도구다.
*예: "아침에 눈을 뜨" 다음에 "면"이 올 확률 70%, "고"가 올 확률 10%… 이런 식으로 모든 글자에 확률을 매긴다.*

**대규모 언어모델 (Large Language Model, LLM)** — 위 언어모델을, 가중치 수와 학습 데이터를 엄청나게 키운 것이다. GPT가 대표적인 예이며, 구조의 원리는 이 프로젝트의 작은 모델과 완전히 같다.

**토크나이저 (Tokenizer)** — 텍스트를 숫자로, 숫자를 다시 텍스트로 바꿔주는 사전이다. 모델은 글자를 직접 이해하지 못하고 오직 숫자만 다루기 때문에 반드시 필요하다.
*예: "안녕" → [12, 87] 로 바꾸고, 나중에 [12, 87] → "안녕" 으로 되돌린다.*

**토큰 (Token)** — 모델이 다루는 가장 작은 단위다. 이 프로젝트에서는 토큰 하나가 글자 하나지만, 진짜 GPT에서는 자주 쓰이는 단어 조각(예: "-습니다")이 토큰 하나가 된다.

**어휘 (Vocabulary, vocab)** — 모델이 아는 서로 다른 토큰의 전체 목록이다. 우리 코퍼스에는 서로 다른 글자가 277개 있으므로 어휘 크기는 277이다.

**코퍼스 (Corpus)** — 모델을 학습시키는 데 쓰는 텍스트 뭉치 전체다. 이 프로젝트에서는 `corpus.txt`가 코퍼스이며, 아무 텍스트 파일로나 바꿔도 된다.

**임베딩 (Embedding)** — 글자 번호(예: 176) 같은 무의미한 숫자를, 의미를 담을 수 있는 여러 개의 숫자로 된 벡터로 바꾼 것이다. 학습을 거치면 비슷한 역할의 글자끼리 벡터가 서로 가까워진다.
*예: 글자 하나를 24개의 숫자로 표현 → [0.1, -0.3, 0.8, …]*

**추론 (Inference)** — 이미 학습이 끝난 모델을 실제로 사용하는 단계다. 학습(training)이 모델을 만드는 과정이라면, 추론은 만들어진 모델로 답을 뽑아내는 과정이다.

**생성 (Generation)** — 모델이 다음 토큰을 하나 뽑고, 그걸 입력에 이어 붙여 또 다음 토큰을 뽑기를 반복해 글을 만들어내는 것이다.

**샘플링 (Sampling)** — 모델이 내놓은 확률 분포를 보고, 그중에서 다음 토큰 하나를 실제로 골라 뽑는 행위다. 항상 확률이 가장 높은 것만 뽑으면 매번 똑같은 글만 나오므로, 보통 확률에 비례해 무작위로 뽑는다.

---

## 2. 모델 부품

**가중치 / 파라미터 (Weight / Parameter)** — 모델 안에 들어 있는, 학습을 통해 값이 바뀌어 가는 숫자들이다. 이 숫자들의 최종 값이 곧 모델이 배운 "지식"이다. GPT는 이런 숫자를 수천억 개 가지고 있다.

**편향 (Bias)** — 계산 결과에 일정하게 더해주는 보정용 숫자다. 가중치와 함께 학습으로 조정된다.

**로짓 (Logit)** — softmax를 거치기 전의, 각 토큰이 얼마나 유력한지를 나타내는 "날 점수"다. 아직 확률이 아니라 그냥 크고 작은 점수일 뿐이다.

**소프트맥스 (Softmax)** — 여러 개의 점수(logit)를, 전부 더하면 1이 되는 확률 분포로 바꿔주는 함수다. 이렇게 해야 "각 글자가 나올 확률"로 해석할 수 있다.

**은닉층 (Hidden layer)** — 입력과 출력 사이에 있는 중간 계산 단계다. 은닉층이 있어야 모델이 단순한 관계를 넘어 복잡한 패턴을 학습할 수 있다.

**활성화 함수 (Activation function, tanh)** — 계산에 "구부러짐(비선형)"을 넣어주는 함수다. 이것이 없으면 층을 아무리 많이 쌓아도 결국 하나의 단순한 직선 변환과 같아져서, 복잡한 것을 못 배운다.

**MLP (Multi-Layer Perceptron, 다층 퍼셉트론)** — 선형 변환과 활성화 함수를 번갈아 쌓은, 가장 기본적인 형태의 신경망이다. 이 프로젝트의 2단계 모델이자, 트랜스포머 안에도 한 부품으로 들어간다.

**트랜스포머 (Transformer)** — 현재 거의 모든 LLM이 쓰는 신경망 구조의 이름이다. 핵심 부품인 self-attention 덕분에 긴 문맥을 잘 다룬다.

**셀프 어텐션 (Self-Attention)** — 문장 안의 각 위치가 "다음 글자를 예측하려면 앞의 어느 글자들을 얼마나 참고해야 할까?"를 스스로 계산해서, 관련 있는 위치의 정보를 골라 끌어오는 장치다. 트랜스포머의 핵심 발명이다.
*예: "…비가 자주 내린다. 빗소리는…"에서 "빗"을 예측할 때, 앞의 "비"에 크게 주목한다.*

**멀티헤드 어텐션 (Multi-head Attention)** — 위의 self-attention을 한 번만 하는 대신, 서로 다른 관점으로 보는 여러 개의 attention을 동시에 돌리고 그 결과를 합치는 방식이다. 한 head는 문법 관계에, 다른 head는 의미 관계에 주목하는 식으로, 여러 종류의 연관성을 한꺼번에 포착할 수 있다. (이 프로젝트는 단순화를 위해 head 1개만 쓴다.)

**코잘 마스크 (Causal mask)** — 위치 t가 자기 자신과 그 앞(과거)의 글자만 볼 수 있고, 뒤(미래)의 글자는 못 보게 가리는 장치다. 다음 글자를 맞히는 훈련에서 정답인 미래 글자를 미리 훔쳐보면 학습이 무의미해지므로 반드시 필요하다.

**레이어놈 (Layer Normalization, LayerNorm)** — 각 위치의 숫자 벡터를 평균 0, 분산 1이 되도록 정규화해서 값의 크기를 일정하게 맞추는 장치다. 이렇게 하면 학습이 훨씬 안정적이고 빨라진다.

**잔차 연결 (Residual connection)** — 어떤 계산 f를 한 뒤, 그 결과에 원래 입력을 다시 더해주는 방식이다(수식으로 `x = x + f(x)`). 깊은 신경망에서 기울기(학습 신호)가 사라지지 않고 끝까지 잘 흐르도록 해주는 "고속도로" 역할을 한다.

**문맥 길이 (Context length / Block size)** — 모델이 다음 글자를 예측할 때 한 번에 참고하는 앞 토큰의 개수다. 이 값이 클수록 더 먼 과거까지 참고할 수 있지만 계산량도 늘어난다.

**위치 임베딩 (Positional embedding)** — 각 토큰이 문장에서 몇 번째 위치인지를 알려주는 벡터다. attention 자체는 순서를 모르기 때문에, 위치 정보를 따로 더해줘야 "첫 글자"와 "열 번째 글자"를 구분할 수 있다.

---

## 3. 학습 (여기가 핵심)

**학습 / 훈련 (Training)** — loss가 낮아지도록 모델의 가중치를 조금씩 바꿔 나가는 전체 과정이다. 아래의 순전파 → loss → 역전파 → 갱신을 수천~수백만 번 반복하는 것이 학습이다.

**손실 (Loss)** — 모델의 예측이 정답과 얼마나 다른지를 하나의 숫자로 잰 값이다. 낮을수록 잘 맞히는 것이며, 학습의 목표는 이 숫자를 낮추는 것이다.

**교차 엔트로피 (Cross-entropy)** — 언어모델에서 쓰는 loss의 종류다. "모델이 정답 글자에 매긴 확률"에 -log를 씌워서, 정답에 확신했으면 작은 값, 헷갈렸으면 큰 값이 되도록 만든 것이다.
*예: 정답 확률 1.0이면 loss 0, 정답 확률 0.32면 loss 약 1.13.*

**순전파 (Forward pass)** — 입력을 넣어서 예측(다음 글자 확률)을 계산해 나가는 방향이다. 입력 → 출력 순서로 진행된다.

**역전파 (Backpropagation)** — "각 가중치를 아주 조금 바꾸면 loss가 얼마나 변할까?"를, 출력에서 입력 방향으로 거꾸로 계산하는 과정이다. 그 결과로 모든 가중치의 기울기(gradient)를 얻는다.

**연쇄법칙 (Chain rule)** — 역전파가 기대는 미적분 규칙이다. 여러 단계를 거친 계산의 미분을, 각 단계의 미분을 차례로 곱해서 이어 붙이는 방법이다.

**기울기 / 그래디언트 (Gradient)** — 역전파가 내놓는 결과로, 각 가중치에 대해 "어느 방향으로 얼마나 밀면 loss가 늘어나는가"를 나타내는 숫자다. 이 값의 반대 방향으로 가면 loss가 줄어든다.

**경사하강법 (Gradient descent)** — 기울기의 반대 방향으로 가중치를 조금씩 옮겨서 loss를 낮춰 가는 방법이다. 산에서 가장 가파른 내리막을 따라 한 걸음씩 내려가는 것에 비유된다.

**학습률 (Learning rate, LR)** — 경사하강에서 한 번에 얼마나 크게 걸음을 옮길지 정하는 값이다. 너무 크면 최저점을 지나쳐 튕기고, 너무 작으면 학습이 너무 느려진다.

**스텝 / 반복 (Step / Iteration)** — 학습 루프를 한 번 도는 것이다. 한 스텝은 순전파 → loss → 역전파 → 갱신으로 이루어진다.

**배치 / 미니배치 (Batch / Mini-batch)** — 한 스텝에서 함께 사용하는 학습 예시의 묶음이다. 예시 하나씩보다 여러 개를 묶어 평균 내면 학습이 더 안정적이고 빠르다.

**에폭 (Epoch)** — 학습 데이터 전체를 한 번 다 훑는 것을 1 에폭이라고 한다. (이 프로젝트는 스텝 단위로만 설명했지만 같은 개념 계열이다.)

**옵티마이저 (Optimizer)** — 기울기를 받아서 가중치를 실제로 어떻게 갱신할지 정하는 방법이다. 가장 단순한 것이 SGD(확률적 경사하강)다.

**아담 (Adam)** — 널리 쓰이는 옵티마이저로, 각 가중치마다 상황에 맞는 실효 학습률을 자동으로 조절해 SGD보다 빠르고 안정적으로 수렴시킨다. 이 프로젝트의 트랜스포머가 사용한다.

**하이퍼파라미터 (Hyperparameter)** — 학습으로 바뀌는 값이 아니라, 사람이 미리 정해 주는 설정값이다. 학습률, 임베딩 차원, 층 수, 문맥 길이 등이 여기 속한다. (가중치와 헷갈리기 쉬운데, 가중치는 학습이 바꾸고 하이퍼파라미터는 사람이 정한다.)

**과적합 (Overfitting)** — 모델이 학습 데이터를 통째로 외워 버려서, 학습 데이터에는 매우 잘하지만 처음 보는 데이터에는 약해지는 상태다. 이 프로젝트는 코퍼스가 작아서 일부러 이 현상이 잘 나타난다.

**일반화 (Generalization)** — 데이터를 외우는 것이 아니라, 규칙을 파악해서 처음 보는 상황에도 잘 대응하는 능력이다. 이것이 학습의 진짜 목표이며, 방대한 데이터가 있어야 잘 이루어진다.

**퍼플렉서티 (Perplexity)** — loss를 더 직관적으로 바꾼 값으로, `e^loss`로 계산한다. "모델이 다음 글자를 고를 때 헷갈리는 후보의 개수"로 해석할 수 있다.
*예: loss 5.62면 약 277개 중 못 좁힘, loss 4.03이면 약 57개로 좁힘, loss 0.23이면 약 1.25개로 사실상 답을 앎.*

---

## 4. 검증 · 도구

**수치미분 (Numerical gradient)** — 가중치 하나를 아주 조금(예: 0.00001) 흔들어서 loss가 얼마나 변하는지를 직접 재어 기울기를 구하는 방법이다. 느리지만 미분의 정의 그대로라서 확실하며, 역전파가 맞는지 검증하는 기준으로 쓴다.

**그래디언트 체크 (Gradient check)** — 손으로 구현한 역전파의 결과를 수치미분 결과와 비교해서, 역전파에 버그가 없는지 확인하는 검증 절차다. 두 값의 상대오차가 아주 작으면(예: 0.00000001) 역전파가 정확하다는 뜻이다.

**시드 / 씨앗 (Seed)** — 컴퓨터가 만드는 가짜 난수의 출발점이 되는 숫자다. 같은 씨앗을 주면 언제 어디서 돌려도 똑같은 난수 열이 나오므로 결과가 동일해진다. (내 실행 결과와 당신 실행 결과가 같았던 이유가 바로 이것이다.)

**의사난수 (Pseudo-random number, PRNG)** — 정해진 계산식으로 만들어 내는, "무작위처럼 보이지만 실제로는 씨앗에 의해 완전히 결정되는" 숫자다. 진짜 무작위가 아니다.

**재현성 (Reproducibility)** — 같은 코드를 언제 돌려도 같은 결과가 나오도록 보장하는 성질이다. 실험을 비교하고 버그를 찾기 위해 씨앗을 고정해 재현성을 확보한다.

---

## 5. 여기서 진짜 LLM으로 확장하려면

**BPE (Byte Pair Encoding)** — 실전에서 쓰는 토크나이저 방식이다. 자주 함께 등장하는 글자 덩어리를 하나의 토큰으로 묶어서, 같은 문장을 더 적은 수의 토큰으로 표현해 효율을 높인다. 글자 단위보다 시퀀스가 짧아진다.

**층 쌓기 (Stacking layers)** — 트랜스포머 블록을 여러 개 위로 쌓는 것이다. 층이 깊어질수록 더 추상적이고 복잡한 패턴을 배울 수 있다. (이 프로젝트는 블록 1개만 쓴다.)

**RLHF (Reinforcement Learning from Human Feedback, 인간 피드백 기반 강화학습)** — 대량 데이터로 학습을 마친 모델을, 사람이 선호하는 답변 방식에 맞게 추가로 다듬는 정렬(alignment) 단계다. ChatGPT 같은 대화형 모델이 유용하고 안전하게 답하도록 만드는 데 쓰인다.

**정렬 (Alignment)** — 모델의 행동을 사람의 의도·가치에 맞추는 작업 전반을 가리킨다. RLHF가 그 대표적인 방법이다.
